import { FourColumnPipe } from './four-column.pipe';

describe('FourColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new FourColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
